{
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "npx serve -s build -l tcp://0.0.0.0:$PORT",
    "healthcheckPath": "/"
  }
}
